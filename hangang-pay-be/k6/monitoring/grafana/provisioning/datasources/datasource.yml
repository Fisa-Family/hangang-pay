# Grafana 부팅 시 Prometheus 데이터소스 자동 등록
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090 # 같은 compose 네트워크 내 서비스명
    isDefault: true
