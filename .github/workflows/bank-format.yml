name: Bank Format Check

on:
  pull_request:
    branches: [main, dev, test]
    paths:
      - "hangang-pay-bank/**"
      - ".github/workflows/bank-format.yml"

permissions:
  contents: read

jobs:
  bank-format:
    name: Bank Spotless
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          java-version: "17"
          distribution: temurin
          cache: gradle

      - name: Grant Gradle permission
        working-directory: hangang-pay-bank
        run: chmod +x ./gradlew

      - name: Spotless check
        working-directory: hangang-pay-bank
        run: ./gradlew spotlessCheck --no-daemon
